# HudElement

- **Место**: `gta2/Game/HudElement/`
- **Файлы**: `.cpp` 10 строк, `.h` 34 строк
- **Методов**: 10
- **Полностью реализовано**: 0
- **Частично (TODO/незавершено)**: 0
- **Заглушки**: 1
- **Объявлено, но не определено**: 9
- **Реализация в целом: ~0%** (полные + 0.5*частичные)

## Заглушки (мини-тела / return-заглушки)

| метод | строка | строк кода | адрес из дампа |
|---|---|---|---|
| `CheckPedCondition()` | 7 | 3 |  |

## Объявлены в .h, но не определены

- `CheckTargetVisible()`
- `ClearCar()`
- `ClearPed()`
- `Draw()`
- `DrawCarArrow()`
- `DrawPedArrow()`
- `RenderWithReset()`
- `SetColor()`
- `Update()`
